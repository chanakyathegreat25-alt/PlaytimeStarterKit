[gd_scene load_steps=27 format=3 uid="uid://dgwhev5imypr8"]

[ext_resource type="Script" uid="uid://biwuwhtwoexaw" path="res://Interface/Pause/pause.gd" id="1_tymi6"]
[ext_resource type="Theme" uid="uid://c3y4p2f8djdyb" path="res://Interface/MainMenu/Themes/title_button.tres" id="2_2wx6c"]
[ext_resource type="FontFile" uid="uid://cfvfiw1qtrep1" path="res://Interface/Font/OverusedGrotesk-Bold.otf" id="2_atxbg"]
[ext_resource type="Texture2D" uid="uid://ba641twe6vsbv" path="res://Interface/MainMenu/Texture/T_PauseBG.png" id="2_sa6jc"]
[ext_resource type="Texture2D" uid="uid://dxbnbrib3wakg" path="res://TEMP/Screenshot 2025-11-01 124342.png" id="4_nh1wh"]
[ext_resource type="FontFile" uid="uid://d02qiqkper325" path="res://Interface/Font/OverusedGrotesk-Medium.otf" id="5_sa6jc"]
[ext_resource type="Texture2D" uid="uid://bivri32i53cow" path="res://Interface/Loading/LoadGameMenu/Objective.png" id="6_0mkij"]
[ext_resource type="Script" uid="uid://cbvj3eu82rfm1" path="res://Interface/MainMenu/button_current.gd" id="6_nh1wh"]
[ext_resource type="Texture2D" uid="uid://ccao7o5vuq77u" path="res://Interface/Icons/Keyboard/T_ESC.png" id="6_ubcy4"]
[ext_resource type="PackedScene" uid="uid://wted730xx6x5" path="res://Interface/Settings/settings_menu.tscn" id="9_x7wnv"]
[ext_resource type="PackedScene" uid="uid://c0igyvhisp714" path="res://Interface/Loading/LoadGameMenu/load_game.tscn" id="10_nh1wh"]
[ext_resource type="PackedScene" uid="uid://dtpairjbrlucx" path="res://Interface/menu_popup.tscn" id="11_nh1wh"]
[ext_resource type="Script" uid="uid://syb4cas6oqdo" path="res://Interface/MainMenu/button_sfx_player.gd" id="13_4ga5b"]
[ext_resource type="AudioStream" uid="uid://cr1h44t3w130f" path="res://Interface/MainMenu/Sound/hover.wav" id="14_y7c0k"]
[ext_resource type="AudioStream" uid="uid://c7wgcc6jimvtc" path="res://Interface/MainMenu/Sound/SW_SFX_Menu_Forward.wav" id="15_bbrqu"]
[ext_resource type="AudioStream" uid="uid://d3q8178oe4ycy" path="res://Interface/MainMenu/Sound/SW_SFX_Menu_Backward.wav" id="16_jf1wv"]
[ext_resource type="AudioStream" uid="uid://bebahieimis82" path="res://Interface/MainMenu/Sound/select2.wav" id="17_4i5ma"]
[ext_resource type="AudioStream" uid="uid://d1u6111mtf4qu" path="res://Interface/MainMenu/Sound/SW_SFX_Closing_Menu.wav" id="18_y7c0k"]
[ext_resource type="AudioStream" uid="uid://dfsqafxvufphl" path="res://Interface/Pause/Sound/SW_SFX_Modal_PopUp.wav" id="19_bbrqu"]

[sub_resource type="Shader" id="Shader_2lx6b"]
code = "shader_type canvas_item;

uniform sampler2D SCREEN_TEXTURE : hint_screen_texture, filter_linear_mipmap;

uniform bool overlay = false;

uniform float scanlines_opacity : hint_range(0.0, 1.0) = 0.4;
uniform float scanlines_width : hint_range(0.0, 0.5) = 0.25;
uniform float grille_opacity : hint_range(0.0, 1.0) = 0.3;
uniform vec2 resolution = vec2(640.0, 480.0);

uniform bool pixelate = true;

uniform bool roll = true;
uniform float roll_speed = 8.0;
uniform float roll_size : hint_range(0.0, 100.0) = 15.0;
uniform float roll_variation : hint_range(0.1, 5.0) = 1.8;
uniform float distort_intensity : hint_range(0.0, 0.2) = 0.05;

uniform float noise_opacity : hint_range(0.0, 1.0) = 0.4;
uniform float noise_speed = 5.0;

uniform float static_noise_intensity : hint_range(0.0, 1.0) = 0.06;

uniform float aberration : hint_range(-1.0, 1.0) = 0.03;
uniform float brightness = 1.4;
uniform bool discolor = true;

uniform float warp_amount : hint_range(0.0, 5.0) = 1.0;
uniform bool clip_warp = false;

uniform float vignette_intensity = 0.4;
uniform float vignette_opacity : hint_range(0.0, 1.0) = 0.5;

// --- BLUR ---
uniform float blur_amount : hint_range(0.0, 5.0) = 0.0; // 0 = no blur, 5 = strong blur
// --------------

vec2 random(vec2 uv){
    uv = vec2(dot(uv, vec2(127.1,311.7)), dot(uv, vec2(269.5,183.3)));
    return -1.0 + 2.0 * fract(sin(uv) * 43758.5453123);
}

float noise(vec2 uv) {
    vec2 uv_index = floor(uv);
    vec2 uv_fract = fract(uv);
    vec2 blur = smoothstep(0.0, 1.0, uv_fract);

    return mix(
        mix(dot(random(uv_index + vec2(0.0,0.0)), uv_fract - vec2(0.0,0.0)),
            dot(random(uv_index + vec2(1.0,0.0)), uv_fract - vec2(1.0,0.0)), blur.x),
        mix(dot(random(uv_index + vec2(0.0,1.0)), uv_fract - vec2(0.0,1.0)),
            dot(random(uv_index + vec2(1.0,1.0)), uv_fract - vec2(1.0,1.0)), blur.x),
        blur.y
    ) * 0.5 + 0.5;
}

vec2 warp(vec2 uv){
    vec2 delta = uv - 0.5;
    float delta2 = dot(delta.xy, delta.xy);
    float delta4 = delta2 * delta2;
    float delta_offset = delta4 * warp_amount;
    return uv + delta * delta_offset;
}

float border(vec2 uv){
    float radius = min(warp_amount, 0.08);
    radius = max(min(min(abs(radius * 2.0), abs(1.0)), abs(1.0)), 1e-5);
    vec2 abs_uv = abs(uv * 2.0 - 1.0) - vec2(1.0, 1.0) + radius;
    float dist = length(max(vec2(0.0), abs_uv)) / radius;
    float square = smoothstep(0.96, 1.0, dist);
    return clamp(1.0 - square, 0.0, 1.0);
}

float vignette(vec2 uv){
    uv *= 1.0 - uv.xy;
    float vignette = uv.x * uv.y * 15.0;
    return pow(vignette, vignette_intensity * vignette_opacity);
}

// --- BLUR FUNCTION ---
vec4 apply_blur(sampler2D tex, vec2 uv, vec2 res, float amount) {
    if (amount <= 0.001) return texture(tex, uv);
    vec2 texel = 1.0 / res;
    vec4 col = vec4(0.0);
    float total = 0.0;
    // 9-sample Gaussian blur approximation
    for (float x = -2.0; x <= 2.0; x++) {
        for (float y = -2.0; y <= 2.0; y++) {
            float weight = exp(-(x*x + y*y) / (2.0 * amount));
            col += texture(tex, uv + vec2(x, y) * texel * amount) * weight;
            total += weight;
        }
    }
    return col / total;
}
// ----------------------

void fragment()
{
    vec2 uv = overlay ? warp(SCREEN_UV) : warp(UV);
    vec2 text_uv = uv;
    vec2 roll_uv = vec2(0.0);
    float time = roll ? TIME : 0.0;

    if (pixelate){
        text_uv = ceil(uv * resolution) / resolution;
    }

    float roll_line = 0.0;
    if (roll || noise_opacity > 0.0){
        roll_line = smoothstep(0.3, 0.9, sin(uv.y * roll_size - (time * roll_speed)));
        roll_line *= roll_line * smoothstep(0.3, 0.9, sin(uv.y * roll_size * roll_variation - (time * roll_speed * roll_variation)));
        roll_uv = vec2((roll_line * distort_intensity * (1.-UV.x)), 0.0);
    }

    vec4 text;
    if (roll){
        text.r = texture(SCREEN_TEXTURE, text_uv + roll_uv * 0.8 + vec2(aberration, 0.0) * .1).r;
        text.g = texture(SCREEN_TEXTURE, text_uv + roll_uv * 1.2 - vec2(aberration, 0.0) * .1).g;
        text.b = texture(SCREEN_TEXTURE, text_uv + roll_uv).b;
        text.a = 1.0;
    } else {
        text.r = texture(SCREEN_TEXTURE, text_uv + vec2(aberration, 0.0) * .1).r;
        text.g = texture(SCREEN_TEXTURE, text_uv - vec2(aberration, 0.0) * .1).g;
        text.b = texture(SCREEN_TEXTURE, text_uv).b;
        text.a = 1.0;
    }

    // --- APPLY BLUR HERE ---
    text = apply_blur(SCREEN_TEXTURE, text_uv, resolution, blur_amount);
    // -----------------------

    float r = text.r;
    float g = text.g;
    float b = text.b;

    uv = warp(UV);

    if (grille_opacity > 0.0){
        float g_r = smoothstep(0.85, 0.95, abs(sin(uv.x * (resolution.x * 3.14159265))));
        r = mix(r, r * g_r, grille_opacity);

        float g_g = smoothstep(0.85, 0.95, abs(sin(1.05 + uv.x * (resolution.x * 3.14159265))));
        g = mix(g, g * g_g, grille_opacity);

        float b_b = smoothstep(0.85, 0.95, abs(sin(2.1 + uv.x * (resolution.x * 3.14159265))));
        b = mix(b, b * b_b, grille_opacity);
    }

    text.r = clamp(r * brightness, 0.0, 1.0);
    text.g = clamp(g * brightness, 0.0, 1.0);
    text.b = clamp(b * brightness, 0.0, 1.0);

    float scanlines = 0.5;
    if (scanlines_opacity > 0.0){
        scanlines = smoothstep(scanlines_width, scanlines_width + 0.5, abs(sin(uv.y * (resolution.y * 3.14159265))));
        text.rgb = mix(text.rgb, text.rgb * vec3(scanlines), scanlines_opacity);
    }

    if (noise_opacity > 0.0){
        float n = smoothstep(0.4, 0.5, noise(uv * vec2(2.0, 200.0) + vec2(10.0, (TIME * (noise_speed)))));
        roll_line *= n * scanlines * clamp(random((ceil(uv * resolution) / resolution) + vec2(TIME * 0.8, 0.0)).x + 0.8, 0.0, 1.0);
        text.rgb = clamp(mix(text.rgb, text.rgb + roll_line, noise_opacity), vec3(0.0), vec3(1.0));
    }

    if (static_noise_intensity > 0.0){
        text.rgb += clamp(random((ceil(uv * resolution) / resolution) + fract(TIME)).x, 0.0, 1.0) * static_noise_intensity;
    }

    text.rgb *= border(uv);
    text.rgb *= vignette(uv);

    if (clip_warp){
        text.a = border(uv);
    }

    if (discolor){
        float saturation = 0.5;
        float contrast = 1.2;
        vec3 greyscale = vec3(text.r + text.g + text.b) / 3.;
        text.rgb = mix(text.rgb, greyscale, saturation);

        float midpoint = pow(0.5, 2.2);
        text.rgb = (text.rgb - vec3(midpoint)) * contrast + midpoint;
    }

    COLOR = text;
}
"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_y7c0k"]
shader = SubResource("Shader_2lx6b")
shader_parameter/overlay = true
shader_parameter/scanlines_opacity = 0.0
shader_parameter/scanlines_width = 0.25
shader_parameter/grille_opacity = 0.3
shader_parameter/resolution = Vector2(640, 480)
shader_parameter/pixelate = false
shader_parameter/roll = false
shader_parameter/roll_speed = 8.0
shader_parameter/roll_size = 15.0
shader_parameter/roll_variation = 1.8
shader_parameter/distort_intensity = 0.05
shader_parameter/noise_opacity = 0.0
shader_parameter/noise_speed = 0.4949999999953434
shader_parameter/static_noise_intensity = 0.06
shader_parameter/aberration = 0.00100004754750004
shader_parameter/brightness = 1.3000000000029104
shader_parameter/discolor = false
shader_parameter/warp_amount = 0.0
shader_parameter/clip_warp = false
shader_parameter/vignette_intensity = 0.10000000000582077
shader_parameter/vignette_opacity = 0.400000019
shader_parameter/blur_amount = 0.5000000237500001

[sub_resource type="StyleBoxEmpty" id="StyleBoxEmpty_3do1l"]

[sub_resource type="StyleBoxEmpty" id="StyleBoxEmpty_xtlhd"]

[sub_resource type="StyleBoxEmpty" id="StyleBoxEmpty_bslv7"]

[sub_resource type="StyleBoxEmpty" id="StyleBoxEmpty_4ga5b"]

[sub_resource type="StyleBoxLine" id="StyleBoxLine_w20tc"]
color = Color(2.5027991e-06, 0.61833435, 0.75786704, 1)
thickness = 4

[node name="Pause" type="Control"]
process_mode = 3
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
script = ExtResource("1_tymi6")

[node name="ColorRect2" type="ColorRect" parent="."]
material = SubResource("ShaderMaterial_y7c0k")
layout_mode = 0
offset_left = 1920.0
offset_top = 8.392334e-05
offset_right = 2999.9998
offset_bottom = 1920.0001
rotation = 1.5707964

[node name="ColorRect" type="ColorRect" parent="."]
modulate = Color(1, 1, 1, 0.23529412)
layout_mode = 1
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
color = Color(0, 0, 0, 1)

[node name="Background" type="Sprite2D" parent="."]
position = Vector2(962, 540)
scale = Vector2(1.00208, 1)
texture = ExtResource("2_sa6jc")

[node name="Screenshot2025-03-02171422" type="Sprite2D" parent="."]
visible = false
position = Vector2(961, 539)
scale = Vector2(0.75, 0.75)
texture = ExtResource("4_nh1wh")

[node name="Title" type="Label" parent="."]
layout_mode = 0
offset_left = 76.0
offset_top = 37.0
offset_right = 479.0
offset_bottom = 173.0
theme_override_colors/font_color = Color(0.29411766, 0.8117647, 1, 1)
theme_override_fonts/font = ExtResource("2_atxbg")
theme_override_font_sizes/font_size = 81
text = "PAUSED"
horizontal_alignment = 2
vertical_alignment = 1
uppercase = true

[node name="backSF3" type="Button" parent="."]
layout_mode = 0
offset_left = 1758.0
offset_top = 1009.0
offset_right = 1956.0769
offset_bottom = 1073.0
scale = Vector2(0.52, 0.52)
theme_override_colors/font_color = Color(1, 1, 1, 1)
theme_override_colors/font_outline_color = Color(1, 1, 1, 1)
theme_override_fonts/font = ExtResource("5_sa6jc")
theme_override_font_sizes/font_size = 42
theme_override_styles/normal = SubResource("StyleBoxEmpty_3do1l")
theme_override_styles/pressed = SubResource("StyleBoxEmpty_xtlhd")
theme_override_styles/hover = SubResource("StyleBoxEmpty_bslv7")
theme_override_styles/focus = SubResource("StyleBoxEmpty_4ga5b")
text = "BACK"
icon = ExtResource("6_ubcy4")

[node name="Seperator" type="HSeparator" parent="."]
layout_mode = 0
offset_left = -3.0
offset_top = 127.0
offset_right = 479.0
offset_bottom = 173.0
theme_override_styles/separator = SubResource("StyleBoxLine_w20tc")

[node name="buttons" type="Node2D" parent="."]

[node name="VBoxContainer" type="VBoxContainer" parent="buttons"]
offset_left = 216.0
offset_top = 423.0
offset_right = 641.0
offset_bottom = 938.0
alignment = 2

[node name="ResumeNSF" type="Button" parent="buttons/VBoxContainer"]
custom_minimum_size = Vector2(200, 56)
layout_mode = 2
theme = ExtResource("2_2wx6c")
text = "RESUME"
alignment = 0

[node name="CheckpointNSF" type="Button" parent="buttons/VBoxContainer"]
custom_minimum_size = Vector2(200, 56)
layout_mode = 2
theme = ExtResource("2_2wx6c")
text = "RESTART CHECKPOINT"
alignment = 0

[node name="Load" type="Button" parent="buttons/VBoxContainer"]
custom_minimum_size = Vector2(200, 56)
layout_mode = 2
theme = ExtResource("2_2wx6c")
text = "LOAD GAME"
alignment = 0

[node name="SettingsSF2" type="Button" parent="buttons/VBoxContainer"]
custom_minimum_size = Vector2(200, 56)
layout_mode = 2
theme = ExtResource("2_2wx6c")
text = "SETTINGS"
alignment = 0

[node name="MainNSF" type="Button" parent="buttons/VBoxContainer"]
custom_minimum_size = Vector2(200, 56)
layout_mode = 2
theme = ExtResource("2_2wx6c")
text = "MAIN MENU"
alignment = 0

[node name="QuitNSF" type="Button" parent="buttons/VBoxContainer"]
custom_minimum_size = Vector2(200, 56)
layout_mode = 2
theme = ExtResource("2_2wx6c")
text = "QUIT GAME"
alignment = 0

[node name="Current" type="Node2D" parent="buttons"]
visible = false
position = Vector2(168, 642)
script = ExtResource("6_nh1wh")
root_path = NodePath("../VBoxContainer")
button_offset = -49.0

[node name="ColorRect" type="ColorRect" parent="buttons/Current"]
offset_right = 12.0
offset_bottom = 56.0
color = Color(0.29411766, 0.8117647, 1, 1)

[node name="Objective" type="Node2D" parent="."]
position = Vector2(1865, 53)

[node name="Label" type="Label" parent="Objective"]
offset_left = -620.0
offset_top = 32.0
offset_right = -17.0
offset_bottom = 131.0
scale = Vector2(0.91, 0.91)
theme_override_colors/font_color = Color(0.9372549, 0.8039216, 0.16078432, 1)
theme_override_fonts/font = ExtResource("2_atxbg")
theme_override_font_sizes/font_size = 30
text = "Investigate the control room"
horizontal_alignment = 2
uppercase = true

[node name="Objective2" type="Sprite2D" parent="Objective"]
modulate = Color(0.9372549, 0.8039216, 0.16078432, 1)
position = Vector2(-32, 53)
scale = Vector2(0.191406, 0.1875)
texture = ExtResource("6_0mkij")

[node name="SettingsMenu" parent="." instance=ExtResource("9_x7wnv")]
visible = false
layout_mode = 1
menu_popup = NodePath("../menu_popup")

[node name="LoadGame" parent="." instance=ExtResource("10_nh1wh")]
visible = false
layout_mode = 1
menu_popup_path = NodePath("../menu_popup")

[node name="menu_popup" parent="." instance=ExtResource("11_nh1wh")]
layout_mode = 1

[node name="ButtonSFXPlayer" type="Node" parent="."]
script = ExtResource("13_4ga5b")
root_path = NodePath("../buttons")

[node name="Hover" type="AudioStreamPlayer" parent="ButtonSFXPlayer"]
stream = ExtResource("14_y7c0k")
volume_db = -5.0

[node name="Forward" type="AudioStreamPlayer" parent="ButtonSFXPlayer"]
stream = ExtResource("15_bbrqu")

[node name="Backward" type="AudioStreamPlayer" parent="ButtonSFXPlayer"]
stream = ExtResource("16_jf1wv")

[node name="Forward2" type="AudioStreamPlayer" parent="ButtonSFXPlayer"]
stream = ExtResource("17_4i5ma")

[node name="ExitMenu" type="AudioStreamPlayer" parent="ButtonSFXPlayer"]
stream = ExtResource("18_y7c0k")

[node name="Opened" type="AudioStreamPlayer" parent="."]
stream = ExtResource("19_bbrqu")

[connection signal="pressed" from="buttons/VBoxContainer/ResumeNSF" to="." method="_on_resume_pressed"]
[connection signal="pressed" from="buttons/VBoxContainer/CheckpointNSF" to="." method="_on_restart_pressed"]
[connection signal="pressed" from="buttons/VBoxContainer/Load" to="." method="_on_load_pressed"]
[connection signal="pressed" from="buttons/VBoxContainer/SettingsSF2" to="." method="_on_settings_pressed"]
[connection signal="pressed" from="buttons/VBoxContainer/MainNSF" to="." method="_on_main_pressed"]
[connection signal="pressed" from="buttons/VBoxContainer/QuitNSF" to="." method="_on_quit_pressed"]
